<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
    crossorigin="anonymous"
    />
    <!-- <link href="../public/css/styles.css" rel="stylesheet" type="text/css" /> -->
    <title>Formulario de Prueba</title>
  </head>
  
  <body>
    <navbar class="navbar navbar-dark bg-dark mb-4">
      <div class="container">
        <h1 class="navbar-brand">FORMULARIO DE PRUEBA MONGODB</h1>
      </div>
    </navbar>
    
    <div class="container">
      <div class="row">
        <div class="col-md-5">
          <div class="container">
            <div class="flexbox ">      <form id="form-afiliado">
              <input id="nombre" type="text" name="nombre" placeholder="Nombre" />
              <input
              id="apellido"
              type="text"
              name="apellido"
              placeholder="Apellido"
              />
              <input id="edad" type="number" name="edad" placeholder="Edad" />
              <button type="submit">Enviar</button>
              <div type="button" class="btn btn-primary btn-familiar" onclick="postForm(); agregarFamiliar(); ocultarBotones();">AGREGAR FAMILIAR</div>
              <div type="button" class="btn btn-primary btn-familiar" onclick="postForm();">POST FORM</div>
            </form>
          </div>
        </div>
        
        <h1>Asi como está encuentra el scripts.js sino diria 404 en la consola, pero por algun motivo no ejecuta la funcion decirHola</h1>

        <div class="col-md-7">
          <ul>
            <% afiliados.forEach( afiliado => { %>
              <li><%= afiliado.nombre; %></li>
              <p>edad: <%= afiliado.edad; %></p>
              <a class="btn btn-primary" href="/editarAfiliado/<%=afiliado._id%>">Editar</a>
              <!-- va al editarAfiliado del routes, no al .ejs, desp el del routes si va al .ejs -->
              <a class="btn btn-danger" href="/eliminarAfiliado/<%=afiliado._id%>">Eliminar</a>
              <% if (!afiliado.done) { %>
                <a class="btn btn-success" href="/toggleDone/<%=afiliado._id%>">Done</a>
                <% } else { %>
                  <a class="btn btn-warning" href="/toggleDone/<%=afiliado._id%>">Undone</a>
                  <% }}); %>
      </ul>
    </div>
  </div>
</container>
<script src="scripts/scripts.js"></script>
</body>
</html>
